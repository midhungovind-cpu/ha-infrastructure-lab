# Fictional server seed images

Each directory represents one simulated server. The checked-in files are safe fictional examples for reviewing the intended layout; the browser creates a separate mutable virtual filesystem for every node and persists runtime changes in local storage.

No file in this tree is read from or written to a real server. Do not place production exports, credentials, IP addresses, domains, customer data, or certificate material here.
